<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="64dp"
    android:layout_height="30dp">

    <TextView
        android:id="@+id/countSizeList"
        android:layout_width="24dp"
        android:layout_height="30dp"
        android:layout_alignParentEnd="true"
        android:text="10"
        android:textSize="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/sizeListNext"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/sizeListNext"
        android:layout_width="20dp"
        android:layout_height="30dp"
        android:layout_toStartOf="@+id/countSizeList"
        android:src="@drawable/ic_next"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        tools:srcCompat="@drawable/ic_next" />

    <ImageButton
        android:id="@+id/sizeListBefore"
        android:layout_width="20dp"
        android:layout_height="30dp"
        android:layout_toEndOf="@+id/countSizeList"
        android:src="@drawable/ic_before"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/countSizeList"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/ic_before" />
</androidx.constraintlayout.widget.ConstraintLayout>